# 오디세이 서바이벌 (Odyssey Survival)

뱀파이어 서바이벌의 게임플레이에 하데스의 내러티브를 얹은 모바일 로그라이크입니다.
TypeScript + Canvas 2D로 만들었고, Capacitor로 iOS / Android에 패키징합니다.

> A mobile roguelite survivors game: Vampire Survivors gameplay, Greek-myth
> narrative. TypeScript + Canvas 2D, packaged for iOS/Android with Capacitor.
> Korean / English / Japanese / Chinese.

---

## 직접 플레이해 보기

### 1. 폰에서 바로 (가장 쉬움)

`main`에 푸시되면 GitHub Actions가 자동으로 빌드해 GitHub Pages에 올립니다.
**최초 1회만** 레포 설정을 켜 주세요.

1. GitHub 레포 → **Settings → Pages**
2. **Source** 를 `GitHub Actions` 로 변경
3. **Actions** 탭에서 `Deploy to GitHub Pages` 가 초록불이 되면
   `https://<사용자명>.github.io/odyssey-survival/` 에서 바로 플레이할 수 있습니다.

폰 브라우저로 그 주소를 열고 "홈 화면에 추가"를 하면 주소창 없이 전체화면으로 뜹니다.

### 2. 내 컴퓨터에서

```bash
npm install
npm run dev -- --host      # 예: http://192.168.0.10:5173
```

`--host` 를 붙이면 같은 와이파이에 있는 폰에서 터미널에 찍힌 주소로 접속할 수 있습니다.
데스크톱 브라우저에서는 WASD / 방향키로도 움직일 수 있고, 크롬 개발자도구의 기기
모드(⌘⇧M / Ctrl+Shift+M)로 폰 화면 비율을 흉내 낼 수 있습니다.

### 3. 프로덕션 빌드 확인

```bash
npm run build      # 타입 검사 + dist/ 생성
npm run preview    # http://localhost:4173 에서 dist/ 서빙
```

### 4. 실기기 네이티브 빌드

아래 **모바일 패키징** 절을 참고하세요. Android는 Android Studio,
iOS는 macOS + Xcode가 필요합니다.

---

## 게임 디자인

### 조작

화면 **아무 곳이나 드래그**하면 그 방향으로 이동하고, 영웅은 **나아가는 방향으로 자동
공격**합니다. PC에서는 **방향키 또는 WASD**로 똑같이 움직입니다. 손가락이 닿은 지점이 스틱의 원점이 되고, 멀리 끌면 원점이 따라옵니다.
휴대폰에서 정확히 조준하기는 어렵기 때문에 전방 약 16° 안에 적이 있으면 공격이 그쪽으로
살짝 붙습니다(에임 어시스트). 조준은 여전히 "이동 방향"으로 하는 것입니다.

발밑의 화살표가 지금 쏘는 방향을 알려 줍니다.

### 영웅

| 영웅       | 무기          | 성격                               | 해금      |
| ---------- | ------------- | ---------------------------------- | --------- |
| 오디세우스 | 이타케의 활   | 긴 사거리, 관통, 표준형            | 처음부터  |
| 아킬레우스 | 펠리온의 창검 | 전방 광역 참격, 강한 넉백, 근접    | 350 골드  |
| 시지프스   | 영원의 바위   | 느린 관통 투사체 + 착탄 광역       | 900 골드  |
| 타나토스   | 죽음의 장막   | 조준 불필요한 지속 장판, 빈사 즉사 | 1600 골드 |

다중 사격(`atk_count`)은 영웅마다 다르게 해석됩니다. 활/바위는 투사체가 늘고, 아킬레우스는
참격이 좌우로 늘어나며, 타나토스는 **회전하는 낫**을 얻습니다.

### 보물상자와 카드

맵을 돌아다니면 **체력을 가진 보물상자**가 나타납니다. 공격으로 부수면 3장 중 1장을 고릅니다.

- **신의 축복** — 15주신 × 각 3종 × 3단계(아테나 아이기스만 4단계).
- **기본 공격 업그레이드** — 피해, 공격 속도, 사거리, 크기, 투사체 개수, 관통, 유도,
  치명타, 그리고 **신의 각인**.

**레벨업**은 별도로, 영웅 스탯 카드를 3장 중 1장 고릅니다.

### 신의 각인 (Divine Infusion)

보물상자에서 나오는 **전설 등급 무기 강화**입니다. 축복이 아니라 무기 강화이고,
축복을 최소 하나 이상 받은 뒤에만 등장합니다.

각인은 **기본 공격에 효과를 얹는 것**이지, 따로 나가는 공격이 아닙니다. 여러 신을
모셨다면 공격할 때마다 신이 번갈아 실리고, **투사체 색깔이 그 신의 색으로 바뀌므로**
무엇이 터질지 날아가는 동안 알 수 있습니다.

**"각인 위력 50/85/130%"** 는 신마다 정해진 효과의 세기에 곱해지는 값입니다.
예를 들어 제우스 각인은 적중 지점에 기본 공격 피해의 45%를 방사하는데, 위력 130%에서는
그 45%가 그만큼 커지고 반경도 함께 넓어집니다.

각인은 **그 신의 축복과 겹치지 않는 축**으로 설계했습니다. 아레스 축복이 출혈이면 각인은
처형(빈사 적 추가 피해), 하데스 축복이 파멸 각인이면 각인은 최대 체력 비례 피해, 제우스
축복이 옆으로 튀는 사슬이면 각인은 한 점에서 터지는 방사입니다. 헤르메스 각인은 쿨다운을
건드리지 않고 **기본 공격을 한 번 더 나가게** 합니다. 15신 전부의 각인 효과는 게임 안
**만신전** 화면에서 읽을 수 있습니다.

### 만신전 (신 해금)

15주신 중 **아레스 · 아테나 · 헤르메스 · 가이아 · 포세이돈**은 처음부터 사용할 수 있고,
나머지는 골드로 모십니다. 모시지 않은 신의 축복은 보물상자에 **아예 나오지 않습니다.**

| 신              | 가격  | 결                                       |
| --------------- | ----- | ---------------------------------------- |
| 하데스 💀       | 400   | 처치 시 회복 · 최후의 폭발 · 파멸 각인   |
| 아폴론 ☀️       | 600   | 활 강화 · 치유의 찬가 · 관통하는 빛줄기  |
| 아프로디테 💗   | 700   | 약화 · 상심 폭발 · 매혹                  |
| 헤파이스토스 🔨 | 850   | 화상 · 갑주 · 청동 자동인형              |
| 제우스 ⚡       | 1,000 | 정전기 · 사슬 번개 · 심판의 벼락         |
| 헤스티아 🔥     | 1,150 | 화롯불 · 온기 · 꺼지지 않는 불(1회 부활) |
| 디오니소스 🍷   | 1,300 | 흡혈 · 넝쿨 속박 · 체력 비례 피해        |
| 헤라 👑         | 1,500 | 위압 오라 · 질투의 낙인 · 혼인의 서약    |
| 아르테미스 🌙   | 1,700 | 치명타 · 달빛 유도 화살 · 치명타 광역    |
| 데메테르 ❄️     | 2,000 | 빙결 · 풍요 · 겨울의 심판                |

만신전 화면은 상점이자 도감입니다. 사기 전에 그 신의 축복 3종과 각인 효과를 전부 읽을 수
있습니다 — 15주신 판테온에서 깜깜이 해금은 그냥 복권이니까요.

### 신탁의 그릇 — 왜 신이 하나뿐인가

한 항해에서 축복을 받을 수 있는 **신의 종류**는 처음에 **1종**입니다. 영구 강화의
「신탁의 그릇」으로 2종 → 3종까지 늘어납니다.

처음 하는 사람은 여기서 반드시 의아해하므로, 게임이 스스로 설명합니다. 그릇이 찬 상태로
상자를 열면 카드 밑에 그릇 상태(`2/3`)와 한 줄 설명이 붙고, **새 신은 「교체」 카드로
등장합니다** — 어떤 신을 놓게 되는지 카드에 그대로 적혀 있어서, 그릇이 하나여도 판이
굳지 않습니다. 타이틀 화면에는 다음에 해금할 신과 가격이 항상 한 줄로 떠 있습니다.

### 카드가 어떻게 작동하는지 (카드마다 표시)

카드 하나하나에 **어떻게 작동하는지**가 붙어 있습니다. 신의 축복이 기본 공격을 바꾸는
건지, 알아서 따로 나가는 건지 헷갈리지 않도록.

| 표시      | 뜻                                                              |
| --------- | --------------------------------------------------------------- |
| 기본 공격 | 기본 공격에 효과가 얹힌다 (예: 서리, 해일, 파멸 각인)           |
| 자동 발동 | 주기적으로 알아서 나간다 (예: 심판의 벼락, 가시덩굴, 신탁의 빛) |
| 지속 효과 | 항상 적용된다 (예: 전술안, 여왕의 위엄)                         |
| 조건 발동 | 처치하거나 맞았을 때 터진다 (예: 최후의 숨결, 정전기 폭풍)      |

### 영구 강화 (메타 진행)

항해가 끝나면 획득한 골드가 남습니다. 골드로 **영구 강화**를 삽니다.

핵심은 **신탁의 그릇**입니다. 처음에는 한 판에서 신의 축복을 **1종류**만 받을 수 있고,
골드를 모아 단계적으로 **2종 → 3종**까지 해금합니다. 그 외에 시작 체력, 피해, 이동 속도,
골드/경험치 획득, 부활 횟수, 상자 출현률, 시작 레벨을 올릴 수 있습니다.

### 난이도

적 체력은 `1 + 분^1.25 × 0.11`, 스폰 속도는 분당 구간별로 오릅니다. 5분마다 보스
(미노타우로스 / 케르베로스)가 등장하고, 25분까지 내러티브 이정표가 흘러갑니다.

---

## 프로젝트 구조

```
src/
  core/         엔진: 고정 timestep 루프, 드래그 입력, 카메라/렌더러,
                시드 RNG, 절차적 WebAudio 효과음, localStorage 세이브
  data/         콘텐츠 테이블: 영웅, 15주신, 축복 45종, 무기 강화 9종,
                레벨업 특성 12종, 영구 강화, 적/웨이브/보스 스케줄
  game/         시뮬레이션: 런 상태, 엔티티, 공간 해시, 전투/효과,
                로드아웃 계산과 카드 추첨, 캔버스 드로잉, 수익화 계층
  ui/           DOM 오버레이: 화면들, HUD, 런타임 생성 아이콘
  i18n/         ko / en / ja / zh 사전과 t() 런타임
tests/          Playwright 기반 스모크 · 플로우 · 소크 테스트
scripts/        게임 데이터에서 능력 도감(docs/CODEX.md)을 생성
```

이미지 에셋이 하나도 없습니다. 영웅별 실루엣이 살아 있는 벡터 스프라이트부터 신의 문양,
카드 아이콘, 적, 이펙트까지 모두 런타임에 캔버스로 그리고 효과음도 WebAudio로 합성합니다.
덕분에 설치 용량을 거의 늘리지 않으면서도 모바일 웹에서 즉시 뜹니다.

애니메이션도 시뮬레이션이 이미 들고 있는 두 개의 시계로 만듭니다 — 이동 중에만 도는
`walkPhase`(걸음 흔들림)와 `attackProgress`(0이면 방금 휘두름, 1이면 다음 공격 준비 완료).
그림을 스프라이트로 바꾸고 싶다면 `src/game/render.ts` 의 그리기 함수만 갈아끼우면
됩니다. 시뮬레이션은 렌더러를 전혀 모릅니다.

### 콘텐츠 추가하기

- **새 축복**: `src/data/boons.ts`에 항목 하나를 추가합니다. 이름/설명은 `L(ko, en, ja, zh)`로
  네 언어를 같이 적고, `apply(out, level)`에서 `out.stats` / `out.mech`를 건드립니다.
  `apply`는 "그 레벨을 보유한 상태의 전체 효과"이며, 카드를 얻을 때마다 로드아웃을 처음부터
  다시 계산하므로 값이 어긋날 일이 없습니다.
- **새 메커니즘**: `src/game/stats.ts`의 `Mechanics`에 숫자 필드를 추가하고(0 = 꺼짐)
  `src/game/run.ts`에서 읽습니다.
- **새 신**: `src/data/gods.ts`에 색·이모지·가격·각인 설명을, `src/data/boons.ts`에 축복
  3종을 추가하면 만신전·카드·HUD에 자동으로 나타납니다.
- **새 영웅**: `src/data/heroes.ts`에 정의를 추가하고, 새 무기 타입이면 `Run.attack()`과
  `drawWeapon()`에 분기를 하나씩 추가합니다.
- **새 언어**: `src/i18n/`에 사전을 추가하고 `LOCALES`에 등록합니다. 사전은 한국어를
  기준으로 타입이 잡혀 있어 빠진 키가 있으면 컴파일에 실패합니다.

---

## 다국어

한국어 · English · 日本語 · 中文을 지원합니다. 처음 실행할 때 기기 언어를 감지하고,
설정에서 언제든 바꿀 수 있습니다.

UI 문구는 `src/i18n/*.ts` 사전에, **콘텐츠 문구(축복 이름·설명 등)는 수치 바로 옆**에
`L()`로 적습니다. 밸런스 숫자와 그 설명이 같은 곳에 있어야 어긋나지 않기 때문입니다.

---

## 비즈니스 모델 (지금은 꺼져 있음)

수익화 코드는 완성돼 있지만 MVP에서는 **꺼둔 상태**입니다.
`src/game/monetization.ts` 의 `MONETIZATION_ENABLED` 하나로 상점 · 보상형 광고 부활 ·
결과 화면 골드 2배가 전부 켜지고 꺼집니다. 꺼져 있어도 코드는 그대로 컴파일·타입 검사되므로
방치되는 동안 썩지 않습니다.

붙일 때가 되면 게임 코드는 건드리지 않고 두 개의 구현만 갈아끼우면 됩니다.

```ts
// src/app.ts
this.money = new Monetization(
  new AdMobProvider(), // AdProvider 구현 (@capacitor-community/admob)
  new RevenueCatBilling(), // BillingProvider 구현
  () => this.save,
  () => this.commit(),
);
```

- **보상형 광고** — 상점에서 하루 5회까지 골드, 사망 시 1회 부활, 결과 화면 골드 2배.
- **인앱 결제** — 골드 꾸러미, 광고 제거(보상 2배), 영웅 전원 즉시 해금.

`AdProvider`는 `isReady(placement)`와 `showRewarded(placement)`만 만족하면 되고,
`showRewarded`는 **보상을 실제로 받았을 때만** `true`를 돌려줘야 합니다.
`BillingProvider`는 `isAvailable()`, `list()`, `purchase(id)`, `restore()`를 구현합니다.

---

## 모바일 패키징 (Capacitor)

```bash
npm run build
npx cap add android      # 최초 1회
npx cap add ios          # 최초 1회 (macOS + Xcode 필요)
npm run cap:android      # 빌드 + 동기화 + Android Studio 열기
npm run cap:ios          # 빌드 + 동기화 + Xcode 열기
```

`capacitor.config.ts`에 앱 ID(`com.odyssey.survival`), 배경색, `webDir: 'dist'`가 있습니다.
네이티브 프로젝트에서 추가로 손볼 것:

- **세로 고정** — Android는 `android/app/src/main/AndroidManifest.xml`의 액티비티에
  `android:screenOrientation="portrait"`, iOS는 Xcode의 Deployment Info에서 Portrait만 체크.
- **노치 대응** — CSS에 `env(safe-area-inset-*)`가 이미 적용돼 있고, `index.html`에
  `viewport-fit=cover`가 들어 있습니다.
- **진동** — 웹에서는 `navigator.vibrate`, 네이티브 앱에서는 `@capacitor/haptics`를 자동으로
  사용합니다. 일반 타격·치명타·피격·방어·보스 경고·사망을 서로 다른 강도와 패턴으로
  구분합니다. 설정에서 진동을 끄거나 강도를 고를 수 있습니다.
- **접근성** — 설정에서 화면 흔들림과 강한 섬광을 함께 줄일 수 있습니다. 이 옵션은 전투
  판정이나 다른 시각 정보에는 영향을 주지 않습니다.

---

## 능력 도감

축복 45종·무기 강화·특성·영구 강화의 **단계별 수치 전부**가
[`docs/CODEX.md`](docs/CODEX.md) 에 있습니다.

이 파일은 손으로 쓴 게 아니라 **게임 데이터에서 직접 생성**됩니다. 밸런스를 고쳤으면
다시 뽑으면 되고, 그래서 문서가 코드와 어긋날 일이 없습니다.

```bash
npm run codex          # 한국어 → docs/CODEX.md
npm run codex -- en    # 영어   → docs/CODEX.en.md
```

## 테스트

세 개의 Playwright 스크립트가 있습니다. 전부 실제 브라우저에서 게임을 **직접 플레이**합니다.

```bash
npm run build && npm run preview     # 다른 터미널에서

npm run test:smoke   # 폰 크기로 한 판 플레이 → 상자 파괴 → 카드 선택 → 일시정지
npm run test:flows   # 네 영웅 전투, 사망→결과, 부활, 영구 강화 구매, 신 해금, 언어 전환
```

```bash
npm run dev                          # 다른 터미널에서
npm run test:soak    # 네 영웅을 각각 25분(시뮬레이션 시간) 빨리 감아
                     # 난이도 곡선, 보스 스케줄, 카드 풀, 신 개수 제한 검증
```

소크 테스트는 렌더링 없이 시뮬레이션만 빨리 감는 개발용 훅(`window.odyssey.simulate`)을
쓰기 때문에 **개발 서버에서만** 동작합니다. 프로덕션 번들에는 포함되지 않습니다
(빨리 감기 치트가 되기 때문입니다).

`OUT=./shots npm run test:smoke`처럼 `OUT`을 주면 각 단계의 스크린샷을 남깁니다.
CI(`.github/workflows/ci.yml`)는 매 PR마다 타입 검사 · 빌드 · 스모크 테스트를 돌립니다.

---

## 스토어 출시 준비

App Store · Google Play · Steam을 목표로 한다면, 지금 코드 기준으로 남은 일은 이렇습니다.

### iOS / Android — Capacitor

아래 **모바일 패키징** 절차대로 하면 바로 빌드됩니다. 심사 전에 필요한 것:

- **아이콘 · 스플래시** — `@capacitor/assets` 로 원본 1024×1024 하나에서 전부 생성.
- **개인정보 처리방침 URL** — 두 스토어 모두 필수. 광고/결제를 켤 때는 수집 항목도 명시.
- **연령 등급** — 폭력 표현 기준으로 iOS 9+/12+, Google Play는 IARC 설문.
- **App Tracking Transparency** — iOS에서 광고 SDK를 붙이는 순간 필요.
- **`android/app/build.gradle`** 의 `versionCode`/`versionName`, iOS의 빌드 번호 관리.
- 현재 세이브는 `localStorage` 입니다. 기기 변경 시 진행도가 날아가므로,
  출시 전에 `@capacitor/preferences` 또는 클라우드 세이브로 옮기는 것을 권합니다.

### Steam — 데스크톱 래퍼

Capacitor는 데스크톱을 만들지 않습니다. 같은 `dist/` 를 그대로 쓰는 방법이 두 가지입니다.

- **Tauri** — 번들 3~10MB, Rust 툴체인 필요. 지금처럼 가벼운 웹 게임에 가장 잘 맞습니다.
- **Electron** — 번들 80MB+, 설정이 가장 쉽고 Steamworks 예제도 많습니다.

키보드 조작은 이미 들어가 있어서(방향키/WASD) 데스크톱에서 바로 플레이됩니다.
Steam 심사에 추가로 필요한 것:

- **Steamworks SDK 연동** — 업적/클라우드 세이브를 쓸 경우. `steamworks.js`(Node) 또는
  Tauri 플러그인.
- **창 크기 대응** — 캔버스는 이미 반응형이지만, 초광폭(21:9)에서 시야가 넓어져
  난이도가 내려갑니다. 카메라 줌 상한을 걸어야 합니다 (`src/core/renderer.ts`).
- **게임패드** — 현재 미지원. `DragInput` 에 Gamepad API 축 입력을 추가하면 됩니다.
- 스토어 페이지 에셋(카드 이미지, 트레일러, 스크린샷 5장 이상).

### 세 스토어 공통

- **폰트** — Cinzel 라틴 서브셋과 Noto Sans KR 한글 서브셋을 번들합니다. 일본어·중국어는
  아직 시스템 글꼴을 사용하므로, 해당 언어에서도 일관된 인상을 내려면 전용 서브셋을
  추가하는 것이 좋습니다.
- **효과음** — 전부 WebAudio 합성음입니다. 출시 전에 실제 사운드로 교체하는 것이 좋습니다.
- **광고/결제** — `MONETIZATION_ENABLED` 를 켜고 실제 SDK를 연결 (아래 참고).

## 앞으로 할 일

- 손그림 스프라이트로 교체 (렌더러가 분리돼 있어 시뮬레이션 수정 없이 가능)
- 영웅별 전용 축복과 보스별 고유 패턴
- 리더보드, 일일 시드 챌린지, 세이브 서버 동기화
- 출시 후 광고 / 결제 SDK 연결 (`MONETIZATION_ENABLED`)
